<%- include('partials/_header') %>

<body>
	<div class="tm-page-wrap mx-auto">
		<div class="position-relative">
			
			<%- include('partials/_navbar') %>

			<div class="tm-welcome-container tm-fixed-header tm-fixed-header-1">
				<div class="text-center">
					<p class="pt-5 px-3 tm-welcome-text tm-welcome-text-2 mb-1 text-white mx-auto">Background image can be fixed. Content will simply slide over.</p>                	
				</div>                
            </div>

			<!-- Header image -->
            <div id="tm-fixed-header-bg"></div> 
		</div>

		<!-- Page content -->
		<div class="container-fluid">
			<div class="mx-auto tm-content-container">
				<main>
					<div class="row mb-5 pb-4">
						<div class="col-12">
							<img src="<%= photo.image %>" class="w-100" />
						</div>
					</div>
					<div class="row mb-5 pb-5">
						<div class="col-xl-8 col-lg-7">
							<!-- Video description -->
							<div class="tm-video-description-box">
								<h2 class="mb-4 tm-video-title"><%= photo.title %></h2>
								<p class="mb-4"><%= photo.description %></p>
							</div>							
						</div>
						<div class="col-xl-4 col-lg-5">
							<!-- Share box -->
							<div class="tm-bg-gray tm-share-box">
								<div class="mb-3">
									<a href="/photo/edit/<%= photo._id %>" class="btn btn-primary p-0 tm-btn-animate tm-btn-download tm-icon-download w-100"><span>Update Detail</span></a>	
								</div>	
								<div>
									<a href="/photo/detail/<%= photo._id %>?_method=DELETE" onclick="return confirm('Silmek istediğinize emin misiniz?');" class="btn btn-danger p-0 tm-btn-animate tm-btn-download tm-icon-download w-100"><span>Delete Photo</span></a>	
								</div>
							</div>
						</div>
					</div>
				</main>

				<%- include('partials/_footer') %>
			</div>
		</div>
	</div>

	<%- include('partials/_script') %>
    <script>
    	$(document).ready(function() {
    		$('.tm-likes-box').click(function(e) {
    			e.preventDefault();
    			$(this).toggleClass('tm-liked');

    			if($(this).hasClass('tm-liked')) {
    				$('#tm-likes-count').html('486 likes');
    			} else {
    				$('#tm-likes-count').html('485 likes');
    			}
    		});
    	});
    </script>
</body>
</html>